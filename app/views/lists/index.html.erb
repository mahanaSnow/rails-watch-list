<div class="container">
  <h1 class="text-center">Movie Lists</h1>
  <div class="row">
    <% @lists.each do |list| %>
      <div class="col-md-4">
        <div class="list-card">
          <%= link_to list.name, list_path(list), style: "text-decoration: none; color: white; font-size: 18px; font-weight: 600;" %>
        </div>
      </div>
    <% end %>
  </div>
</div>
 <div class="text-center">
    <%= link_to 'Retour à l’accueil', root_path, class: 'btn btn-primary' %>
  </div>
